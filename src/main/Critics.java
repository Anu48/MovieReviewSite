package main;

public class Critics extends Users{

    /**
     * Constructs a User that has a userName and userType
     *
     * @param userName the user name
     * @param userType the type of the user
     */
    public Critics(String userName, String userType) {
        super(userName, userType);
    }
}
